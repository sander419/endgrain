/**
 * Размер покупной доски по умолчанию.
 *
 * Хранение переехало в `profile.ts`: покупная доска — настройка мастерской,
 * а не свойство рецепта, и ездит она вместе с остальным профилем.
 * Здесь остался только ходовой размер, от которого начинают.
 */
import type { StockBoard } from './nesting';

export const DEFAULT_STOCK: StockBoard = { lengthMm: 2000, widthMm: 150 };
